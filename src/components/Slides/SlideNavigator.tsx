import type { TweetSlide } from '../../types/project'
export function SlideNavigator({ slides, activeId, select, add, duplicate, remove }: { slides: TweetSlide[]; activeId: string; select: (id: string) => void; add: () => void; duplicate: () => void; remove: () => void }) { return <nav className="slide-nav" aria-label="Slides"><div className="slide-list">{slides.map((slide, i) => <button className={slide.id === activeId ? 'slide-chip active' : 'slide-chip'} onClick={() => select(slide.id)} key={slide.id}>{String(i + 1).padStart(2, '0')}</button>)}</div><div className="slide-actions"><button className="button secondary" onClick={add}>+ Novo</button><button className="button secondary" onClick={duplicate}>Duplicar</button>{slides.length > 1 && <button className="button danger" onClick={remove}>Excluir</button>}</div></nav> }
